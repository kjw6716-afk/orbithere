// Manual editorial placements only. Keep disabled until the real AdSense account
// is ready and its disclosures/consent settings have been configured (docs/ads.md).
window.ORBIT_ADS = Object.freeze({enabled: false, client: '', slot: ''});
